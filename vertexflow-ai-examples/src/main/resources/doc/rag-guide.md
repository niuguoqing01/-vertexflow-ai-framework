# RAG Guide

RAG means Retrieval-Augmented Generation.

In VertexFlow AI Framework, RagEngine loads documents, splits them into chunks, stores chunks in VectorStore, retrieves relevant chunks, and sends the context to ChatModel.

RAG helps AI generate answers based on private or domain-specific knowledge.